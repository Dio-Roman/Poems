<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet")
        //- link(rel="stylesheet", href="/static/css/add.css")
        //- link(rel="stylesheet", href="/static/css/nav-left.css")
        //- link(rel="stylesheet", href="/static/css/footer.css")
        link(rel="stylesheet", href="/static/css-min/add.min.css")
        title Добавить новый стих
    body
        include nav-left.pug
        main.add
            h1.add__title Добавить новый стих
            p Стихов в базе:  
                span.count-poems=  countPoems
            form(action="/upload" method="POST" enctype="multipart/form-data" autocomplete="off")
                .add__left.wrap
                    label.label(for="stihname") Название стиха
                    input(type="text" id="stihname" placeholder="Поставьте пробел, если отсутствует" name="title")
                    label.label(for="stihtext") Текст стиха
                    textarea(id="stihtext" placeholder="..." cols="50" rows="10" name="text")
                .add__right.wrap
                    .radio
                        p.label Выберите жанр
                        label
                            input(type="radio" id="radio1" value="lyrics" name="genre")
                            |лирика
                        label
                            input(type="radio" id="radio2" value="job" name="genre")
                            |о работе
                        label
                            input(type="radio" id="radio3" value="other" name="genre")
                            |другое
                    p.label Имя файла изображения
                    input(id="img-name" type="text" placeholder="Например: 1-fontan" name="img")
                    .btn-choose-file
                        label(for="file") Выберите файл...
                    input.choosefile(id="file" type="file" name="file")
                    input.submit#submit(type="submit" value="Добавить")
                    a.logout(href="/logout", disabled) logout
        include footer.pug
        //- script(src="../static/js/checkandblock.js") 
        script(src="../static/js-min/checkandblock.min.js")             