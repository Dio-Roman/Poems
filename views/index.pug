<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.5.2/animate.min.css") 
        link(href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet")
        link(rel="stylesheet", href="/static/css/nav-left.css")
        link(rel="stylesheet", href="/static/css/main.css")
        link(rel="stylesheet", href="/static/css/footer.css")
        title Стихотворения
    body
        header.header
            .header__logo.animated.rollIn
            nav.header__nav-top
                select.select(onchange="selectIndex()")
                    option(value="all") все 
                    option(value="lyrics") лирика / любовь / женшины 
                    option(value="job") о работе 
                    option(value="other") другое
        include nav-left.pug
        .main
            - let i = 0;
            while i <= mongo.length-1
                a.main__card.wow.fadeInUp(
                    class= mongo[i].genre
                    href="http://localhost:3000/"+mongo[i]._id
                )
                    if mongo[i].title == ' '
                        .main__card-img
                        .main__card-name
                            h3.main__card-title
                            p= mongo[i++].text
                            //- span= mongo[i]._id
                    else
                        .main__card-img(style=`background-image: url(../static/img/${mongo[i].img}.jpeg);`)
                        .main__card-name
                            h3.main__card-title= mongo[i++].title
        include footer.pug
        script(src="../static/js/wow.min.js")
        script new WOW().init();
        script(src="../static/js/css.js")
        script(src="../static/js/activelnk.js")
        script(src="../static/js/auth.js")
                